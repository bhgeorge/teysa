export namespace theme {
    namespace colors {
        const theme_1: {
            bg: string;
            'bg-alt': string;
            text: string;
            'text-alt': string;
            error: string;
            success: string;
            warning: string;
            interact: string;
            'interact-hover': string;
        };
        export { theme_1 as theme };
    }
    namespace fontFamily {
        const sans: string[];
    }
    namespace fontWeights {
        const extralight: string;
        const regular: string;
        const bold: string;
    }
    namespace extend {
        namespace spacing {
            const gutter: string;
            const em: string;
        }
    }
}
export const plugins: never[];
